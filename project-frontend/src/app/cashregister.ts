/* export interface CashRegister {
    id: string;
    name: string;
    balance: number;
} */

export class CashRegister {
    id: string | undefined;
    name: string | undefined;
    balance: number | undefined;
}